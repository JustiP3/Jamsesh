<h1>Tags</h1>

<p><%= render 'search' %></p>
<p><%= link_to "Show All Tags", tags_path %></p>
<br />

<% @tags.each do |tag| %>
    <p>
    <%= tag.name + ": " %><br />
    <% tag.bands.each do |band| %>
        <%= link_to band.name, band_path(band) %><br />
    <% end %>
    </p>    
<% end %>
